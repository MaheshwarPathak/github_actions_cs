name: Trigger Detective

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  detect:
    runs-on: ubuntu-latest

    steps:
      - name: Print triggering event
        run: |
          echo "Event that triggered this workflow: ${{ github.event_name }}"

      - name: Show push branch
        if: github.event_name == 'push'
        run: |
          echo "Branch pushed to: ${{ github.ref_name }}"

      - name: Show pull request branches
        if: github.event_name == 'pull_request'
        run: |
          echo "Source branch: ${{ github.head_ref }}"
          echo "Target branch: ${{ github.base_ref }}"

      - name: Finish detective work
        run: |
          echo "Detective work complete"